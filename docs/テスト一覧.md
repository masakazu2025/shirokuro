# テスト一覧

バックエンド(pytest) 74件、フロントエンド単体(Vitest) 71件、E2E(Playwright) 24件、合計169件。

## バックエンド(pytest, `backend/tests/`)

### test_transaction_search.py(検索API, 49件)

| テスト内容 | 期待する結果 |
|---|---|
| 該当件数がちょうど10000件 | 切り詰めず全件返る |
| 該当件数が10001件(上限超過) | 10000件に切り詰められる |
| shop_noを1件で絞り込み | 一致する取引のみ返る |
| shop_noを複数指定(OR) | いずれかに一致する取引が返る |
| register_noを1件で絞り込み | 一致する取引のみ返る |
| register_noを複数指定(OR) | いずれかに一致する取引が返る |
| ipaddressを1件で絞り込み | 一致する取引のみ返る |
| ipaddressを複数指定(OR) | いずれかに一致する取引が返る |
| transaction_no_fromのみ指定 | 指定値以上の取引が返る |
| transaction_no_toのみ指定 | 指定値以下の取引が返る |
| transaction_no_from〜to指定 | 範囲内の取引が返る |
| transaction_no_from=to(単一値) | その番号の取引だけ返る |
| created_at_fromのみ指定 | 指定日時以降の取引が返る |
| created_at_toのみ指定 | 指定日時以前の取引が返る |
| created_at範囲(同日内の時刻指定) | 時刻境界で正しく絞り込める |
| created_at_toを日付のみ指定 | 同日でも0時より後の取引は含まれない |
| 日付+IP+取引番号単一の複合条件 | 全条件を満たす取引だけ返る |
| 日付+IP+取引番号範囲の複合条件 | 全条件を満たす取引だけ返る |
| created_at_from+IP単一 | 両条件を満たす取引が返る |
| created_at_from+IP複数(OR) | 両条件を満たす取引が返る |
| created_at範囲+IP単一 | 両条件を満たす取引が返る |
| created_at範囲+IP複数(OR) | 両条件を満たす取引が返る |
| 全条件を単一値で指定 | 全条件を満たす取引だけ返る |
| 全条件を複数・範囲値で指定 | 全条件を満たす取引だけ返る |
| 該当なしの条件(shop_no=99999) | 空配列が返る |
| created_at_fromの境界(完全一致) | 境界値の取引を含めて過不足なく返る(>=) |
| created_at_toの境界(完全一致) | 境界値の取引を含めて過不足なく返る(<=) |
| created_at_toの境界(1秒後) | 1秒後の取引は除外される |
| transaction_no_fromの境界(完全一致) | 境界値の取引を含めて過不足なく返る(>=) |
| transaction_no_toの境界(完全一致) | 境界値の取引を過不足なく返る(<=) |
| shop_noのField制約(1・99999は許可、0・100000は拒否) | 許可範囲は200、範囲外は422 |
| transaction_no_from/toのField制約(1・9999は許可、0・10000は拒否) | 許可範囲は200、範囲外は422 |
| 各パラメータに不正な型を渡す(数値に文字列、IPに不正な形式、日付に不正な形式) | 422エラーになる |
| created_at_from > created_at_to(範囲逆転) | 422エラーになる |
| transaction_no_from > transaction_no_to(範囲逆転) | 422エラーになる |
| リスト内の一部だけ不正な値(shop_no=[1, "abc"]) | 422エラーになる |
| 複合条件中の1つだけ不正な値 | 422エラーになる |

### test_transaction.py(取引CRUD・アイテム一覧, 13件)

| テスト内容 | 期待する結果 |
|---|---|
| 取引一覧の取得 | 登録件数が返る |
| 取引の単体取得 | 各フィールドが正しく返る |
| 取引の更新(started_at/ended_at) | 更新後の値が返る |
| 取引の削除 | 削除できる、2回目は404 |
| アイテム一覧の取得 | journal/product_record/payment_recordのlabel・urlが正しい |
| journalアイテムの取得 | transaction_id・nameが正しい |
| 存在しないjournalアイテムの取得 | 404になる |
| product_recordアイテムの取得 | transaction_id・nameが正しい |
| 存在しないproduct_recordアイテムの取得 | 404になる |
| product_recordのデータが不正な形式 | 422になる |
| payment_recordアイテムの取得 | transaction_id・nameが正しい |
| 存在しないpayment_recordアイテムの取得 | 404になる |
| payment_recordのデータが不正な形式 | 422になる |

### test_payment_record.py(支払レコードの整形ロジック, 3件)

| テスト内容 | 期待する結果 |
|---|---|
| 支払レコードのJSON整形 | カテゴリごとにlabel・columns・rowsが正しく組み立てられる |
| category_labelsに未定義のカテゴリ | 日本語ラベルに変換せず生のキーのまま返る |
| 想定と異なる構造のデータ | ItemDataErrorが発生する |

### test_product_record.py(商品レコードの整形ロジック, 2件)

| テスト内容 | 期待する結果 |
|---|---|
| 商品レコードのJSON整形 | columns・rowsが正しく組み立てられる(カテゴリマスタで名称解決) |
| 不正な行データ | ItemDataErrorが発生する |

### test_journal_item.py(ジャーナルの整形ロジック, 1件)

| テスト内容 | 期待する結果 |
|---|---|
| ジャーナルのテキストをJSON化 | type/name/label/dataが正しく返る |

### test_terminal.py(端末CRUD, 未使用エンドポイント, 6件)

| テスト内容 | 期待する結果 |
|---|---|
| 端末の登録 | 登録した内容が返る |
| 端末一覧の取得 | 登録済みのIPが全て返る |
| 端末の削除 | 削除できる、一覧から消える |
| 不正なIPで登録 | 422になる |
| 重複したIPで登録 | 409になる |
| 存在しない端末の削除 | 404になる |

## フロントエンド単体テスト(Vitest, `frontend/src/`)

### hooks/useSearchState.test.js(検索状態管理フック, 32件)

| テスト内容 | 期待する結果 |
|---|---|
| 初期状態 | 端末未選択・当日OFF・日付はデモデータの固定日付(2026-07-14)・詳細検索は閉じている |
| 端末の選択・解除 | terminalsに追加/削除され、canSearchが連動する |
| 端末選択のlocalStorage保存 | 選択・解除のたびに保存内容が更新される |
| 端末選択の復元 | 保存されていた選択状態を起動時に復元する |
| 端末選択の壊れたJSON | 空の選択状態として起動する |
| 複数端末選択時の取引番号入力欄 | グレーアウトされない |
| 詳細検索を開いたときの単一入力欄 | グレーアウトされる(範囲入力の取引番号は除く) |
| 詳細検索を開いたときの日時範囲の自動セット | 単一の日付から00:00〜23:59の範囲を自動で入れる |
| 詳細検索を閉じて日付を変えて再度開く | 新しい日付の範囲に更新される |
| 📌固定でdetailOpenを保存 | localStorageに保存される |
| 固定状態の復元 | detailOpenがtrueで初期化される |
| 固定復元時のグレーアウト連動 | 復元されたdetailOpenに応じてグレーアウトも正しく効く |
| 固定されていない場合の初期化 | detailOpenはfalseになる |
| クエリ組み立て(詳細検索閉) | 日付は終日・取引番号は完全一致で組み立てる |
| クエリ組み立て(詳細検索開) | 範囲入力の値をそのまま使う |
| 複数端末選択時のクエリ組み立て | 取引番号もクエリに含める |
| 取引番号未入力時のクエリ組み立て | クエリに含めない |
| 日時範囲がfrom > to | dateRangeInvalidがtrueになり検索不可 |
| 取引番号範囲がfrom > to | txnRangeInvalidがtrueになり検索不可 |
| from <= toの範囲 | 不正と判定されない |
| 範囲の片方だけ入力 | 不正と判定されない |
| 当日チェックのデフォルト | OFF・日付はデモデータの固定日付・入力欄は編集可能 |
| 当日チェックをON | 日付が今日になり、入力欄が無効化される |
| 当日OFFでsetDate | 任意の日付に変更できる |
| 当日をON→OFF | 日付は今日のまま編集可能になる |
| 当日チェック・日付のlocalStorage保存 | 変更のたびに保存される |
| 当日OFFの保存状態の復元 | 保存されていた日付そのまま復元される |
| 当日ONの保存状態の復元 | 保存日付は使わず、現在の日付を計算し直す |
| クリアボタン | 端末・日付・取引番号・範囲・詳細検索が全て初期状態に戻る |
| クリア後のlocalStorage | 初期状態の内容で保存し直される |

### components/SearchBar.test.jsx(検索バー, 18件)

| テスト内容 | 期待する結果 |
|---|---|
| 端末未選択 | 検索ボタンが無効 |
| 端末選択 | 検索ボタンが有効になる |
| 検索ボタン押下 | 選択端末を含むパラメータでonSearchが呼ばれる |
| 詳細検索を開く | 範囲入力欄が表示され、単一入力欄がグレーアウトする |
| 複数端末選択+詳細検索 | 取引番号の範囲入力欄は無効にならない |
| 取引番号入力欄の制約 | min=1, max=9999 |
| 取引番号範囲入力欄の制約 | min=1, max=9999 |
| 当日チェックのデフォルト | OFF、日付は編集可能でデモデータの日付が入っている |
| 当日チェックを入れる | 日付入力欄が無効化される |
| 詳細検索を開いている間 | 当日チェックボックス自体も無効になる |
| 日時範囲がfrom > to | エラーメッセージ表示、検索ボタン無効化 |
| 取引番号範囲がfrom > to | エラーメッセージ表示、検索ボタン無効化 |
| 範囲をfrom <= toに直す | エラーメッセージが消え、検索ボタンが有効に戻る |
| エラーが無い状態 | メッセージ表示欄の高さは常に確保されている(レイアウトが動かない) |
| クリアボタン押下 | 端末・入力値が全てリセットされる |
| 詳細検索を開閉 | ボタン文言が「詳細検索」⇔「簡易検索」に変わる |
| 詳細検索が開いている間 | ボタンに強調色のクラスが付く |
| 簡易検索ボタン押下 | 単一入力に戻り、範囲入力欄が消える |

### components/TerminalSelect.test.jsx(端末ドロップダウン, 7件)

| テスト内容 | 期待する結果 |
|---|---|
| 端末候補の重複排除 | IP+店舗+レジで重複排除して表示する |
| チェックボックスのクリック | onToggleがそのIPで呼ばれる |
| 未選択時の表示 | 「端末を選択してください」と表示する |
| 1件選択時の表示 | そのIPを表示する |
| selected propとchecked状態の一致 | selectedに含まれるIPのチェックボックスだけがcheckedになる |
| 枠外クリック | ドロップダウンが閉じる |
| 複数選択時の表示 | 「先頭IP 他N件」と表示する |

### components/TransactionList.test.jsx(取引一覧, 3件)

| テスト内容 | 期待する結果 |
|---|---|
| 取引一覧の表示 | 各取引が行として表示される |
| 行のクリック | onSelectがそのtransaction_idで呼ばれる |
| selectedIdとハイライト | 一致する行だけがハイライト表示される |

### components/ItemViewer.test.jsx(アイテム表示, 5件)

| テスト内容 | 期待する結果 |
|---|---|
| tables型の表示 | 決済手段ごとのタブを表示し、先頭タブの内容を表示する |
| tables型のタブ切り替え | クリックした支払手段の内容に切り替わる |
| text型の表示 | そのままテキストで表示する |
| table型の表示 | 単一テーブルとして列見出し・行内容を表示し、タブは出さない |
| 未対応のtype | フォールバックメッセージを表示する |

### hooks/useTheme.test.js(テーマ切り替えフック, 6件)

| テスト内容 | 期待する結果 |
|---|---|
| localStorage未保存(システムがダーク) | ダークモードに従う |
| localStorage未保存(システムがライト) | ライトモードに従う |
| localStorage保存あり | システム設定より保存値を優先する |
| toggleTheme | テーマが切り替わり、localStorageに保存される |
| テーマがdark | `<html>`にdarkクラスが付与される |
| テーマがlight | `<html>`にdarkクラスが付与されない |

## E2Eテスト(Playwright, `frontend/e2e/`)

### search-flow.spec.js(検索の基本フロー, 4件)

| テスト内容 | 期待する結果 |
|---|---|
| 端末未選択 | 検索ボタンが無効 |
| 端末選択→検索 | 実際にバックエンドから取得した一覧が表示される |
| 一覧から取引を選択 | 詳細(ジャーナル等のタブ)が表示される |
| 支払レコードの決済手段タブをクリック | 表示内容(楽天Payの表示/非表示)が実際に切り替わる |

### today-checkbox.spec.js(当日チェック・日付のデフォルト, 4件)

| テスト内容 | 期待する結果 |
|---|---|
| 初回訪問時 | 当日OFF・日付はデモデータの固定日付(2026-07-14) |
| 当日ON | 日付が今日になり、入力欄がグレーアウトする |
| リロード後(当日ON保存済み) | ON状態が復元され、日付は現在日付に再計算される |
| リロード後(当日OFF+任意の日付保存済み) | 保存された日付のままそのまま復元される |

### terminal-select.spec.js(端末選択, 3件)

| テスト内容 | 期待する結果 |
|---|---|
| 複数端末の選択 | 「IP 他N件」の表示になる |
| 枠外クリック | ドロップダウンが閉じる |
| リロード後 | 選択した端末が復元される |

### detail-search.spec.js(詳細検索/簡易検索の切り替え, 3件)

| テスト内容 | 期待する結果 |
|---|---|
| 「詳細検索」を押す | 「簡易検索」に文言・見た目(強調色)が変わる |
| 詳細検索を開く | 既存の日付から日時範囲(00:00〜23:59)が自動セットされる |
| 📌固定を押してリロード | 詳細検索が開いた状態で復元される |

### validation.spec.js(範囲入力のバリデーション, 7件)

| テスト内容 | 期待する結果 |
|---|---|
| 日時範囲がfrom > to | エラー表示され検索ボタンが無効になる |
| 日時範囲を修正 | エラーが消え、検索ボタンが有効に戻る |
| 日時範囲を修正して検索 | デモデータの日付を含む範囲で実際に検索結果が表示される |
| 取引番号範囲がfrom > to | エラー表示され検索ボタンが無効になる |
| 取引番号範囲を修正 | エラーが消え、検索ボタンが有効に戻る |

### clear-button.spec.js(クリアボタン, 1件)

| テスト内容 | 期待する結果 |
|---|---|
| クリアボタン押下 | 端末・日付・詳細検索が全て初期状態に戻る |

### theme.spec.js(テーマ切り替え, 2件)

| テスト内容 | 期待する結果 |
|---|---|
| ライト・ダーク切り替え | 見出し・本文のテキストが背景に対してWCAG AA基準(コントラスト比4.5以上)を満たす |
| リロード後 | テーマ設定(darkクラス・ボタン表示)が復元される |

### error-handling.spec.js(アイテム解析エラー, 2件)

| テスト内容 | 期待する結果 |
|---|---|
| 商品レコード(CSV)の解析エラーがある取引 | エラー(422)が表示される |
| 支払レコード(JSON)の構造エラーがある取引 | エラー(422)が表示される |
